export type ReportData = {
    id: string;
    title: string;
    description: string;
}

export type NewReportData = {
    title: string;
    description: string;
}
    

export type SavedReportData = {
    id: string;
}